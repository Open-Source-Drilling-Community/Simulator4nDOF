apiVersion: v1
kind: Service
metadata:
  name: {{ include "norcedrillingsimulator4ndofwebappclient.fullname" . }}
  labels:
    {{- include "norcedrillingsimulator4ndofwebappclient.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "norcedrillingsimulator4ndofwebappclient.selectorLabels" . | nindent 4 }}
